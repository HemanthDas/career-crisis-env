# OpenEnv — REQUIRED by hackathon judges
openenv-core>=0.2.3

# Web framework (used by create_fastapi_app internally)
fastapi>=0.110.0
uvicorn>=0.29.0

# Data validation (OpenEnv uses Pydantic v2)
pydantic>=2.0.0

# UI
gradio>=4.26.0

# Utilities
python-multipart>=0.0.9
requests>=2.25.0